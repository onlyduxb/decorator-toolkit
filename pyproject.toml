[project]
name = "decorator_toolkit"       # underscores, matches import
version = "0.1.0"
description = "Type-safe decorators for logging, timing, retry, memoization, and validation."
authors = [{name = "Adam Worsnip"}]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"